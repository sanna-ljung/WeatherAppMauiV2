<?xml version="1.0" encoding="UTF-8" ?>
<Shell
    x:Class="WeatherAppMauiV2.AppShell"
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:local="clr-namespace:WeatherAppMauiV2"
    xmlns:views="clr-namespace:WeatherAppMauiV2.Views"
    Shell.FlyoutBehavior="Disabled"
    Title="WeatherAppMauiV2">

    <TabBar>
        <ShellContent
            Title="Väder"
            ContentTemplate="{DataTemplate local:MainPage}"
            Route="MainPage"
            Icon="weather.png" />

        <ShellContent
            Title="Favoriter"
            ContentTemplate="{DataTemplate views:FavoriteCitiesPage}"
            Route="FavoriteCitiesPage"
            Icon="star.png" />
    </TabBar>

</Shell>
